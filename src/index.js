import React from 'react';
import ReactDOM from 'react-dom';
import Render from './NoteApp/Main';

ReactDOM.render(
  <div className="container">
      <Render />
  </div>,
  document.getElementById('root')
);
